Modu³ XBee jest radiowym modu³em s³u¿¹cym do komunikacji na znaczne odleg³oœci. Topologi¹ sieci po³¹czeñ jest siatka (lub siatka czêœciowa). Mo¿na skonfigurowaæ urz¹dzenie do pracy równie¿ w topologiach P2P (peer to peer), PPP (point to point protocol) lub P2MP (point to multipoint). Zaimplementowany protokó³ ZigBee pozwala modu³owi komunikowaæ siê z innymi modu³ami korzystaj¹cymi z ZigBee, nawet innych producentów. Maksymalny zasiêg, przy braku przeszkód, wynosi 1,2 km, co powinno byæ wystarczaj¹cym wynikiem dla u¿ytku lokalnego. Istnieje dro¿sza wersja modu³u XBee PRO, któr¹ cechuje zasiêg 3,2 km. Modu³ XBee mo¿e byæ obs³ugiwany przez mikrokontroler przy u¿yciu SPI lub modu³u UART~\cite{manual:xbee}. U¿ycie protoko³u SPI zosta³o omówione w \hyperref[sec:sub:spi]{Sekcji~\ref*{sec:sub:spi}: Serial Peripheral Interface (SPI)})

\subsection{Universal Asynchronous Receiver/Transmitter (UART)} \label{sec:sub:uart}
Jedn¹ z mo¿liwoœci obs³ugi modu³u XBee jest u¿ycie modu³u UART. Protokó³ obs³ugi modu³u  jest podobny do protoko³u RS232. Sk³ada siê on w warstwie fizycznej przede wszystkim z dwóch asynchronicznych linii danych:
\begin{itemize}
\item DIN - Data In
\item DOUT - Data Out
\end{itemize}
Gdy nie odbywa siê transmisja, linie powinny byæ podci¹gniête do zasilania. Na \hyperref[fig:uart]{Rysunku~\ref*{fig:uart}} przedstawiono przyk³adowy przesy³ wartoœci 0x1F (0b00011111) przy wykorzystaniu komunikacji szeregowej.

\begin{figure}[h]
\centering \includegraphics[width=0.75\textwidth]{figures/uart.JPG}
\caption{Pakiet danych wysy³any przez modu³ UART~\cite{manual:xbee}}\label{fig:uart}
\end{figure}

Transmisja rozpoczyna siê od najm³odszego bitu. Przed rozpoczêciem transmisji nale¿y zdefiniowaæ jej prêdkoœæ, gdy¿ komunikacja jest asynchroniczna. Prêdkoœæ charakteryzuje baudrate, czyli iloœæ zmian sygna³u podczas jednej sekundy. Dodatkowo nale¿y ustawiæ  parzystoœæ przesy³anych bitów oraz iloœæ bitów stopu, które koñcz¹ przesy³anie pojedynczej ramki.\\
Modu³ XBee zawiera bufory: nadawczy oraz odbiorczy. W zale¿noœci od trybu dzia³ania, modu³ mo¿e gromadziæ w buforach dane, czekaj¹c na gotowoœæ do wys³ania wiadomoœci. Do wskazywania przepe³nienia bufora odbiorczego s³u¿y pin CTS. Dodatkowo, pin RTS wskazuje czy mo¿na przesy³aæ kolejne wiadomoœci do odbiorcy. Dobr¹ praktyk¹ jest ³¹czenie pinu RTS nadawcy z CTS odbiorcy~\cite{manual:xbee}.\\
Modu³ mo¿e dzia³aæ w dwóch trybach przetwarzania informacji:
\begin{itemize}
\item transparentny (przezroczysty), który sprawia, ¿e modu³ dzia³a jako konwerter szeregowej linii danych na sygna³ radiowy.  Wys³anie danych z bufora odbiorczego nastêpuje po up³ywie z góry ustalonego interwa³u czasowego, w którym nie odebrano ¿adnej informacji, przy nades³aniu sekwencji znaków GT+CC+GT lub przy przepe³nieniu bufora odbiorczego. Ten tryb dostêpny jest tylko w komunikacji poprzez UART.
\item API (Aplication Programming Interface), który rozbudowuje ramkê odbiorcz¹ i nadawcz¹ o dodatkowe pola. Dziêki temu trybowi mo¿na konfigurowaæ modu³ XBee u¿ywaj¹c zestawu komend do³¹czonych w nocie katalogowej~\cite{manual:xbee}. U³atwia on te¿ adresowanie w przypadku dzia³ania w topologii P2MP lub siatki.
\end{itemize}